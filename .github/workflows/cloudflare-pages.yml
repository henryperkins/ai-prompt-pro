name: Cloudflare Pages CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy to Cloudflare Pages
    permissions:
      contents: read
    env:
      VITE_AUTH_WORKER_URL: ${{ secrets.VITE_AUTH_WORKER_URL }}
      VITE_API_WORKER_URL: ${{ secrets.VITE_API_WORKER_URL }}
      VITE_AGENT_SERVICE_URL: ${{ secrets.VITE_AGENT_SERVICE_URL }}
      VITE_AGENT_PUBLIC_API_KEY: ${{ secrets.VITE_AGENT_PUBLIC_API_KEY }}
      VITE_NEON_PUBLISHABLE_KEY: ${{ secrets.VITE_NEON_PUBLISHABLE_KEY }}
      VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
      VITE_GITHUB_CONTEXT_ENABLED: "false"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Validate frontend build secrets
        run: |
          if [ -z "${{ secrets.VITE_AUTH_WORKER_URL }}" ]; then
            echo "Missing required secret: VITE_AUTH_WORKER_URL"
            exit 1
          fi
          if [ -z "${{ secrets.VITE_API_WORKER_URL }}" ]; then
            echo "Missing required secret: VITE_API_WORKER_URL"
            exit 1
          fi
          if [ -z "${{ secrets.VITE_AGENT_SERVICE_URL }}" ]; then
            echo "Missing required secret: VITE_AGENT_SERVICE_URL"
            exit 1
          fi
          if [ -z "${{ secrets.VITE_AGENT_PUBLIC_API_KEY }}" ] && [ -z "${{ secrets.VITE_NEON_PUBLISHABLE_KEY }}" ] && [ -z "${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}" ]; then
            echo "Missing required secret: VITE_AGENT_PUBLIC_API_KEY (or legacy VITE_NEON_PUBLISHABLE_KEY / VITE_SUPABASE_PUBLISHABLE_KEY)"
            exit 1
          fi

      - name: Validate documentation references
        run: node scripts/check-docs-freshness.mjs

      - name: Run linter
        run: npm run lint

      - name: Run unit tests
        run: npm run test:unit

      - name: Build
        run: npm run build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=promptforge --branch=main

  preview:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    name: Deploy Preview
    permissions:
      contents: read
      pull-requests: write
    env:
      VITE_AUTH_WORKER_URL: ${{ secrets.VITE_AUTH_WORKER_URL }}
      VITE_API_WORKER_URL: ${{ secrets.VITE_API_WORKER_URL }}
      VITE_AGENT_SERVICE_URL: ${{ secrets.VITE_AGENT_SERVICE_URL }}
      VITE_AGENT_PUBLIC_API_KEY: ${{ secrets.VITE_AGENT_PUBLIC_API_KEY }}
      VITE_NEON_PUBLISHABLE_KEY: ${{ secrets.VITE_NEON_PUBLISHABLE_KEY }}
      VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
      VITE_GITHUB_CONTEXT_ENABLED: ${{ secrets.VITE_GITHUB_CONTEXT_ENABLED }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy preview to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=promptforge --branch=preview-${{ github.event.pull_request.number }}

      - name: Comment PR with preview URL
        uses: actions/github-script@v7
        with:
          script: |
            const previewUrl = `https://preview-${{ github.event.pull_request.number }}.promptforge.pages.dev`;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🚀 Preview deployed: ${previewUrl}`
            });
